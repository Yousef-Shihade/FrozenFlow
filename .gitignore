# ---------------------------------------------------------------------------
# Datasets (~3.2 GB) - not committed. See README.md for download instructions.
# ---------------------------------------------------------------------------
Stage_1/Data/

# ---------------------------------------------------------------------------
# Generated feature cache (~46 MB) - regenerate by running notebook 02.
# Derived data, so it is reproducible rather than tracked.
# ---------------------------------------------------------------------------
Stage_1/features/

# ---------------------------------------------------------------------------
# Trained velocity-network weights (~26 MB) - regenerate by running the Stage 2
# notebooks 02 and 03. Derived data, like the feature cache above. Everything
# needed to CHECK them is tracked instead: the per-run accuracies, the training
# curves, and the per-example test predictions they produced.
# ---------------------------------------------------------------------------
Stage_2/results/*_models.pt

# ---------------------------------------------------------------------------
# Not this project's own work / not needed to understand or run it - kept
# locally for reference only.
# ---------------------------------------------------------------------------
Course_Materials/
Stage_1/Mira's Report/
Stage_1/docs/
Stage_2/docs/

# ---------------------------------------------------------------------------
# NOTE on what IS committed, deliberately:
#   Stage_*/Work/*/plots/     figures  - deliverables and evidence of execution
#   Stage_*/Work/*/tables/    CSVs     - the numbers behind every claim
#   Stage_*/Work/*/code/*.ipynb        - WITH outputs, as reproducibility evidence
#   Stage_*/results/                   - aggregated results, prototypes and test
#                                        predictions (the model weights excluded
#                                        above are the only exception)
# ---------------------------------------------------------------------------

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib60/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Virtual environments (ours lives at C:\cvlab_env, outside the repo, but just in case)
.venv/
venv/
env/
ENV/
.conda/

# Jupyter
.ipynb_checkpoints/
*/.ipynb_checkpoints/*
profile_default/
ipython_config.py

# Testing / linting caches
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/

# IDE
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
.idea/
*.swp

# OS
.DS_Store
Thumbs.db
ehthumbs.db
desktop.ini

# Scratch
*.log
*.tmp
scratch/

# Local tooling state
.claude/
