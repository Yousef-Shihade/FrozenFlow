# A Computer Vision Project - Flow Matching as a Layer - Stages 1-3
#
# IMPORTANT: install PyTorch FIRST, from the CUDA index. The default PyPI wheels are
# CPU-only and would silently disable the GPU.
#
#   pip install torch==2.6.0 torchvision==0.21.0 --index-url https://download.pytorch.org/whl/cu124
#   pip install -r requirements.txt
#   pip install -e Stage_1
#   pip install -e Stage_2
#   pip install -e Stage_3
#
# cu124 targets the RTX 3050 Laptop GPU (Ampere, sm_86). Adjust the index URL for a
# different CUDA version; see https://pytorch.org/get-started/locally/

torch==2.6.0
torchvision==0.21.0

numpy>=1.24
pandas>=2.0
matplotlib>=3.7
scikit-learn>=1.3
scipy>=1.10
pillow>=10.0

jupyter>=1.0
ipykernel>=6.0
nbformat>=5.9
nbconvert>=7.0
tqdm>=4.65

pytest>=7.4
